{
  "name": "vibes",
  "private": true,
  "description": "Dev-only test tooling for nyabongo/vibes. The shipped site stays a static file tree with zero build step.",
  "scripts": {
    "docs": "node tools/llms-txt.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "vitest": "^4.1.10"
  }
}
